# --- Phase 1: ingestion + chunking + indexing ---
tree-sitter>=0.23.0
tree-sitter-language-pack>=0.7.0
fastembed>=0.4.0
qdrant-client>=1.12.0
python-dotenv>=1.0.0

# --- Phase 2: generation ---
google-genai>=0.3.0      # default provider (Gemini free tier)
anthropic>=0.40.0        # optional alternative provider

# --- Phase 4: selective agentic loop ---
langgraph>=0.2.0

# --- Phase 5: web service ---
fastapi>=0.110.0
uvicorn>=0.29.0
